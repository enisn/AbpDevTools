# ABP NPM Package Template

Replace placeholders before publishing:

- `__PACKAGE_NAME__`
- `__ABP_VERSION__`
- `__DESCRIPTION__`

Notes:

- Keep `abp.resourcemapping.js` for ABP bundling.
- `abp.resourcemappings.js` is a reminder stub and is safe to keep.
